import styled from "styled-components";
import {font} from "../styles/Common";

export const SectionTitle = styled.h2`
    ${font({weight: 800, Fmax: 120, Fmin: 65, lineHeight: 0.935})}
    text-transform: uppercase;
    max-width: 772px;
    margin-bottom: 70px;
`